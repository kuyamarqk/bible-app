// interfaces.ts

export interface Verse {
    text: string;
    reference: string;
    // Add other properties as needed
  }
  
  export interface VerseIndex {
    index: number;
    // Add other properties as needed
  }
